---
title: humanizeHrs
pagination_label: 'Number Utilities: humanizeHrs'
pagination_next: utilities/numbers/humanize-ms
pagination_prev: utilities/numbers/get-random-number
sidebar_label: humanizeHrs
slug: /utilities/numbers/humanize-hrs
---

import { PackageCaption } from '@site/src/components';

<PackageCaption packageName='@spa-tools/utilities' />

# humanizeHrs()

The `humanizeHrs()` function converts a number of hours into the largest applicable
unit of either year, month, week, or day by returning a tuple of resulting number
with requested decimal places in first position and respective human-readable unit
in second position.

### Usage

```ts
import { humanizeHrs } from '@spa-tools/utilities';

const humanizeHrs1 = humanizeHrs(8760, 0);
console.log(humanizeHrs1);
// --> ['1', 'year']

const humanizeHrs2 = humanizeHrs(730);
console.log(humanizeHrs2);
// --> ['1.0', 'month']

const humanizeHrs3 = humanizeHrs(168, 0);
console.log(humanizeHrs3);
// --> ['1', 'week']

const humanizeHrs4 = humanizeHrs(24);
console.log(humanizeHrs4);
// --> ['1.0', 'day']

const humanizeHrs5 = humanizeHrs(123456, 2);
console.log(humanizeHrs5);
// --> ['14.09', 'years']
```

### Arguments

| Name            | Type     | Required? | Default | Description                           |
| --------------- | -------- | --------- | ------- | ------------------------------------- |
| `hours`         | `number` | yes       | -       | The hours to humanize                 |
| `decimalPlaces` | `number` | no        | `1`     | The decimal places to round/format to |

### Returns

Returns humanized hours as `string`.
