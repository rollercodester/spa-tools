---
title: addDays
pagination_label: 'Date Utilities: addDays'
pagination_next: utilities/dates/add-hours
pagination_prev: utilities/data/pick
sidebar_label: addDays
slug: /utilities/dates/add-days
---

import { PackageCaption } from '@site/src/components';

<PackageCaption packageName='@spa-tools/utilities' />

# addDays()

The `addDays()` function adds a number of days to a given date object,
timestamp integer, or date string.

### Usage

```ts
import { addDays } from '@spa-tools/utilities';

const date = new Date('2021-01-01');

const newDate1 = addDays(date, 5);
console.log(newDate1.toLocaleDateString('en-US'));
// --> '01/06/2021'

const newDate2 = addDays(date.getTime(), 6);
console.log(newDate2.toLocaleDateString('en-US'));
// --> '01/07/2021'

const newDate3 = addDays('2021-01-01', 7);
console.log(newDate3.toLocaleDateString('en-US'));
// --> '01/08/2021'
```

### Arguments

| Name   | Type                       | Required? | Description                                                                            |
| ------ | -------------------------- | --------- | -------------------------------------------------------------------------------------- |
| `date` | `string \| number \| Date` | yes       | The date value to add days to as either date string, timestamp integer, or date object |
| `days` | `number`                   | yes       | The number of days to add                                                              |

### Returns

`Date` - The new date object with the added days.
