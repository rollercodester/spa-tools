---
title: Interaction Hooks
pagination_label: 'Interaction Hooks: Getting Started'
pagination_next: interaction-hooks/use-call-once
pagination_prev: runtime-config/reference
slug: /interaction-hooks
---

import { SiTypescript } from 'react-icons/si';
import { FaCreativeCommonsZero, FaReact } from 'react-icons/fa';
import { LuPackageOpen, LuTrees } from 'react-icons/lu';

import {
  CoreRouterGettingStartedReactTsCode,
  CoreRouterGettingStartedReactJsCode,
  OverviewFeatures,
  SectionText,
} from '@site/src/components';

**@spa-tools/interaction-hooks** is a small package of specialized hooks meant to solve interaction scenarios in modern **React** applications.

<OverviewFeatures
  features={[
    {
      title: 'React Hook',
      icon: <FaReact fontSize='2rem' />,
      titleStyle: { position: 'relative', top: '-0.12rem' },
    },
    {
      title: 'TypeScript First',
      icon: <SiTypescript fontSize='1.75rem' />,
    },
    {
      title: 'Zero Dependencies',
      icon: <FaCreativeCommonsZero fontSize='1.75rem' />,
    },
    {
      title: (
        <>
          4 kB <span style={{ fontWeight: 500 }}>cjs</span> → 9 kB <span style={{ fontWeight: 500 }}>pkg</span>
        </>
      ),
      icon: <LuPackageOpen fontSize='1.75rem' />,
    },
    {
      title: 'Tree-shakable',
      icon: <LuTrees fontSize='2rem' />,
      titleStyle: { position: 'relative', top: '-0.12rem' },
    },
  ]}
/>

## Installation

<SectionText bottomMargin='0' topMargin='0'>
  To install Interaction Hooks in your project, use your favorite package manager and run the respective command:
</SectionText>

```bash npm2yarn
npm install @spa-tools/interaction-hooks
```

## Quickstart

Check out all of the hooks in the sidebar and go for it!
