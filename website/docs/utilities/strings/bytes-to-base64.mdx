---
title: bytesToBase64
pagination_label: 'String Utilities: bytesToBase64'
pagination_next: utilities/strings/capitalize-first-word
pagination_prev: utilities/strings/base64-to-bytes
sidebar_label: bytesToBase64
slug: /utilities/strings/bytes-to-base64
---

import { PackageCaption } from '@site/src/components';

<PackageCaption packageName='@spa-tools/utilities' />

# bytesToBase64()

The `bytesToBase64()` function converts a `Uint8Array` into its equivalent base64 string.

### Usage

```ts
import { bytesToBase64 } from '@spa-tools/utilities';

const byteArray = new Uint8Array([72, 101, 108, 108, 111, 32, 119, 111, 114, 108, 100]);
const base64 = bytesToBase64(byteArray);
console.log(base64);
// --> "SGVsbG8gd29ybGQ="
```

### Arguments

| Name    | Type         | Description               |
| ------- | ------------ | ------------------------- |
| `bytes` | `Uint8Array` | The byte array to convert |

### Returns

Returns a base64 `string`.
