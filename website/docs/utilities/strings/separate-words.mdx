---
title: separateWords
pagination_label: 'String Utilities: separateWords'
pagination_next: utilities/type-helpers/as-strategy
pagination_prev: utilities/strings/make-readable
sidebar_label: separateWords
slug: /utilities/strings/separate-words
---

import { PackageCaption } from '@site/src/components';

<PackageCaption packageName='@spa-tools/utilities' />

# separateWords()

The `separateWords()` function takes a string and separates its proper-cased words by
inserting a separator between them.

### Usage

```ts
import { separateWords } from '@spa-tools/utilities';

const value = 'helloWorld';

const separated1 = separateWords(value);
console.log(separated1);
// --> 'hello World'

const separated2 = separateWords(value, '-');
console.log(separated2);
// --> 'hello-World'

const separated3 = separateWords(value, ' ', true);
console.log(separated3);
// --> 'Hello World'
```

### Arguments

| Name               | Type      | Required? | Default | Description                                         |
| ------------------ | --------- | --------- | ------- | --------------------------------------------------- |
| `str`              | `string`  | yes       | -       | The value to separate                               |
| `separator`        | `string`  | no        | `' '`   | The separator character to insert between words     |
| `ensureProperCase` | `boolean` | no        | `false` | Whether to capitalize the first letter of each word |

### Returns

Returns a `string`.
