---
title: isPromise
pagination_label: 'Conditional Utilities: isPromise'
pagination_next: utilities/conditionals/is-record
pagination_prev: utilities/conditionals/is-past
sidebar_label: isPromise
slug: /utilities/conditionals/is-promise
---

import { PackageCaption } from '@site/src/components';

<PackageCaption packageName='@spa-tools/utilities' />

# isPromise()

The `isPromise()` function is the epitomy of self-describing :-).

### Usage

```ts
import { isPromise } from '@spa-tools/utilities';

const promise = new Promise((resolve) => resolve('foo'));
const nonPromise = { foo: 'bar' };

console.log(isPromise(promise));
// --> true

console.log(isPromise(nonPromise));
// --> false
```

### Arguments

| Name  | Type     | Required? | Description        |
| ----- | -------- | --------- | ------------------ |
| `obj` | `unkown` | yes       | The value to check |

### Returns

`boolean` - If `obj` is `Promise` then returns `true` else returns `false`.
